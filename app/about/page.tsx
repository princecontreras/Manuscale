import React from 'react';
import { AboutPage } from '@/components/AboutPage';

export const metadata = {
    title: 'About Us | Typoscale',
    description: 'Learn about Typoscale, the AI-powered writing platform democratizing book authorship.',
};

export default function About() {
    return <AboutPage />;
}
